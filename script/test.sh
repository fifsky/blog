#!/usr/bin/env bash
set -e
#export selected
#
#APP_HOME="$(cd "$(dirname "${0}")" && cd .. && pwd -P)"
#cd ${APP_HOME}
#
#source ${APP_HOME}/script/util.sh
#
#list=(a b c)
#
#util::select "test" "${list[*]}"
#
#echo $selected
#echo ${list[*]}


a=""

if [ !-n $a]; then
  echo "123"
fi
