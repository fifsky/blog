{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "http",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}",
      "cwd": "${workspaceFolder}",
      "args": ["http", "--addr=:8080"]
    }
  ]
}
